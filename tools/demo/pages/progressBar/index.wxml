<view style="width:100%;height:400px;">
	<comp
		value='{{value}}'
		innerColor='{{innerColor}}'
		outerColor="{{outerColor}}"
		textColor="{{textColor}}"
		text="{{text}}"
		textSize="{{textSize}}"
		loopSize="{{loopSize}}"
		max="{{max}}"
		textSlot="{{false}}"
	/>
	<comp
		value='{{value}}'
		innerColor='{{innerColor}}'
		outerColor="{{outerColor}}"
		textColor="{{textColor}}"
		text="{{text}}"
		textSize="{{textSize}}"
		loopSize="{{loopSize}}"
		max="{{max}}"
		textSlot="{{true}}"
	>
		<view>
			<view style="font-size: 16px">我是第一行</view>
			<view style="font-size: 20px">我是第二行</view>
		</view>
	</comp>
</view>
